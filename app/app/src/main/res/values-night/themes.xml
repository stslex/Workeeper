<resources>
    <!--
      Dark counterpart of values/themes.xml. See that file for why this is a platform theme,
      why android:statusBarColor is absent, and why the background is duplicated as a literal.

      windowBackground is the literal value of AppColors.surfaceTier0 (dark variant).
    -->
    <style name="Theme.Workeeper" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#FF0E0F0E</item>
    </style>
</resources>
