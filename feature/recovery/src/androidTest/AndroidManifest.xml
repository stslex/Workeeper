<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Declares RecoveryActivity for the androidTest apk. The production
        manifest entry lives in app/app/src/main/AndroidManifest.xml referencing
        the same FQCN; this duplicate exists only because feature/recovery
        builds its own test apk in isolation (no app/app manifest is merged
        into the library test target).

        Used exclusively by RecoveryActivityDbFreeTest to verify the DB-free
        invariant — see that test's KDoc.
    -->
    <application>
        <activity
            android:name="io.github.stslex.workeeper.feature.recovery.RecoveryActivity"
            android:exported="false" />
    </application>

</manifest>
